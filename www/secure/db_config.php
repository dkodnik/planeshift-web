<?PHP
    $db_hostname = "localhost";
    $db_username = "planeshift";
    $db_password = "planeshift";
    $db_name = "planeshift";
    $pathToWC = './../webconsole-new/'; // this *must* end in a /, use ./ if the script is in the WC root, otherwise use an additional ../ for every directory it is below that.
?>
